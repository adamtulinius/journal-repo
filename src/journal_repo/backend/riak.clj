(ns journal-repo.backend.riak
  (:require [journal-repo.util :as util]
            [journal-repo.backend :as backend]))


(defn runner
  ""
  [code]
  (println code))

(defn get-checkpoint
  ""
  []
  0)